name: CI

on:
  push:
    branches:
      - main
      - renovate/**
    paths-ignore:
      - "**.md"
  pull_request:
    paths-ignore:
      - "**.md"
  merge_group:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  checks:
    if: >-
      ${{ github.event_name != 'pull_request' ||
          github.event.pull_request.head.repo.full_name != github.repository ||
          !startsWith(github.head_ref, 'renovate/') }}
    name: Run Checks
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install GStreamer
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            gstreamer1.0-plugins-base \
            gstreamer1.0-plugins-bad \
            gstreamer1.0-plugins-good \
            gstreamer1.0-tools \
            libgstreamer1.0-dev \
            libgstreamer-plugins-base1.0-dev \
            libssl-dev

      - name: Setup mise
        uses: jdx/mise-action@v4
        with:
          version: ${{ vars.MISE_VERSION || 'latest' }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check dependency policy
        run: mise run deps:check

      - name: Check dependency ordering
        run: mise run sort:crates:check

      - name: Check formatting
        run: mise run fmt:check

      - name: Run clippy
        run: mise run lint

      - name: Check build
        run: mise run check

      - name: Run tests
        run: mise run test

      - name: Check preview GStreamer plugins
        run: mise run check:gstreamer:preview

      - name: Read minimum supported Rust version
        run: |
          sed -n 's/^rust-version = "\([^"]*\)"$/MSRV=\1/p' Cargo.toml >> "$GITHUB_ENV"

      - name: Cache minimum supported Rust version toolchain
        uses: actions/cache@v5
        with:
          path: ~/.rustup/toolchains/${{ env.MSRV }}-*
          key: rust-msrv-${{ runner.os }}-${{ runner.arch }}-${{ env.MSRV }}

      - name: Install minimum supported Rust version toolchain
        run: mise install "rust@$MSRV"

      - name: Test minimum supported Rust version
        run: cargo +"$MSRV" test --workspace --all-targets --locked

  optional-backends:
    if: >-
      ${{ github.event_name != 'pull_request' ||
          github.event.pull_request.head.repo.full_name != github.repository ||
          !startsWith(github.head_ref, 'renovate/') }}
    name: Check Optional Backends
    runs-on: ${{ vars.RUNNER_MACOS || 'macos-26' }}
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install GStreamer
        run: brew install gstreamer

      - name: Setup mise
        uses: jdx/mise-action@v4
        with:
          version: ${{ vars.MISE_VERSION || 'latest' }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check build with all features
        run: mise run check:all-features

      - name: Run clippy with all features
        run: mise run lint:all-features

      - name: Run tests with all features
        run: mise run test:all-features

      - name: Read minimum supported Rust version
        run: |
          sed -n 's/^rust-version = "\([^"]*\)"$/MSRV=\1/p' Cargo.toml >> "$GITHUB_ENV"

      - name: Cache minimum supported Rust version toolchain
        uses: actions/cache@v5
        with:
          path: ~/.rustup/toolchains/${{ env.MSRV }}-*
          key: rust-msrv-${{ runner.os }}-${{ runner.arch }}-${{ env.MSRV }}

      - name: Install minimum supported Rust version toolchain
        run: mise install "rust@$MSRV"

      - name: Test minimum supported Rust version
        run: cargo +"$MSRV" test --workspace --all-features --locked
